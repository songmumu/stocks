# 交易系统 — 启动与关闭指南

---

## 准备工作

**快捷键 Win + R** → 输入 `cmd` → 回车，打开命令提示符。

以下所有命令均在命令提示符（CMD）窗口中运行。

---

## 一、启动服务

### 步骤 1：启动后端（端口 8001）

```
cd /d C:\Users\admin\.qclaw\workspace-v733kxt9elzfv7u1\trading-system\backend
D:\software\QClaw\v0.2.33.617\resources\python\python.exe -m uvicorn app.main:app --host 0.0.0.0 --port 8001
```

看到以下输出表示启动成功：
```
Uvicorn running on http://0.0.0.0:8001
```

> ⚠️ 不要关闭这个窗口，关闭后服务停止。

---

### 步骤 2：启动前端（端口 5173）

再打开一个**新的**命令提示符窗口：

```
cd /d C:\Users\admin\.qclaw\workspace-v733kxt9elzfv7u1\trading-system\frontend
npm run dev
```

看到以下输出表示启动成功：
```
  VITE v.x.x.x  ready in xxx ms

  Local:   http://localhost:5173/
```

> ⚠️ 同样不要关闭这个窗口。

---

### 步骤 3：验证服务状态

打开浏览器访问：

| 服务 | 地址 | 预期 |
|------|------|------|
| 前端（网页界面） | http://localhost:5173 | 显示交易系统页面 |
| 后端（API） | http://localhost:8001/api/market/indices | 返回 JSON 数据 |

---

## 二、关闭服务

### 方式一：直接关闭窗口（最简单）

关闭后端 CMD 窗口 → 后端停止
关闭前端 CMD 窗口 → 前端停止

### 方式二：通过端口精确终止（推荐）

再打开一个命令提示符窗口：

```
:: 查找并终止后端（8001）
for /f "tokens=5" %a in ('netstat -ano ^| findstr :8001 ^| findstr LISTENING') do taskkill /F /PID %a

:: 查找并终止前端（5173）
for /f "tokens=5" %a in ('netstat -ano ^| findstr :5173 ^| findstr LISTENING') do taskkill /F /PID %a
```

> ✅ 这种方式只关交易系统，不影响 QClaw（QClaw 用的是其他端口）。

---

## 三、常见问题

**Q：浏览器打不开 localhost:5173？**
→ 确认前端 CMD 窗口还在运行，没有报错退出。

**Q：页面显示"网络错误"或数据加载失败？**
→ 确认后端 CMD 窗口还在运行，API 能正常访问。

**Q：端口被占用（Error: Port 8001 in use）？**
→ 先用方式二关掉旧进程，再重新启动。

**Q：想确认端口是否被占用？**
```
netstat -ano | findstr :8001
netstat -ano | findstr :5173
```
有输出说明服务在运行，无输出说明已关闭。

---

## 四、路径速查

| 用途 | 路径 |
|------|------|
| 后端代码目录 | `C:\Users\admin\.qclaw\workspace-v733kxt9elzfv7u1\trading-system\backend` |
| 前端代码目录 | `C:\Users\admin\.qclaw\workspace-v733kxt9elzfv7u1\trading-system\frontend` |
| Python 解释器 | `D:\software\QClaw\v0.2.33.617\resources\python\python.exe` |
| Node.js | `D:\software\QClaw\v0.2.33.617\resources\node\node.exe` |
| 后端端口 | `8001` |
| 前端端口 | `5173` |
